# archive

velog에서 지웠거나 비공개로 돌린 글의 백업이 쌓이는 곳입니다.

- `velog/` — 동기화 시점에 velog 응답에 더는 없는 글의 마크다운
- 이 폴더는 `public/`이 아니라서 빌드 산출물에 들어가지 않습니다. 사이트로 배포되지 않고 URL로도 열리지 않습니다.
- 글이 velog에 다시 공개되면 동기화가 `public/velog/`로 되돌리고 여기서 지웁니다.
